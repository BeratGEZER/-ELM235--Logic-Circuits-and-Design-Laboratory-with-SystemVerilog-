module pos_edge_d_flip_flop (
    input logic D,     // Veri girişi
    input logic clk,   // Saat sinyali
    output logic Q     // Çıkış
);
    always @(posedge clk) begin
        Q <= D;        // Saat sinyalinin pozitif kenarında D'yi Q'ya ata
    end
endmodule
